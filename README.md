# convnet-js

感知器

单向信号   递归神经网络
单个感知器构成了一个简单的决策模型， 已经可以拿来用了。
实际的决策模型复杂很多，是由于多个感知器组成的多层网络。底层感知器的接收外部输入，做出判断后，在发出信号，作为感知器输入，直到得到结果，有可能发生循环传递

权重阀值w
wx + b为 z   z=wx + b  σ(z) = 1 / (1 + e ^ (-z))

output = {0 if ∑j wjxj ≤ threshold
          1 if ∑j wjxj > threshold
